services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: postgres_redirect_statistic
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "8432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: